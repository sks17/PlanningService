CURRENT ROUGH PLAN:

Embedding view — 2D scatter of all frames, colored by cluster, with zoom/pan/lasso selection
Cluster panel — list of clusters with frame counts, condition breakdowns, and an inline rename field (the human-naming workflow)
Frame grid — k-medoid representatives per cluster, click-to-expand into single video at timestamp
Single-video deep dive — one video with biometric overlay (speed/posture as a timeline below), current frame highlighted on the scatter
Condition comparison — side-by-side cluster usage stats with effect-size indicators
Export — CSV with the user's cluster names and per-condition aggregations; PNG/SVG for figures
Persistence — cluster names and view state saved per study, shareable link