LayerChoiceWhyFrontend bundleReact + TS + ViteStandard, fast iterationStatic hostingWhatever BioSyft already usesDon't introduce infraScatter plotdeck.glWebGL, scales to 500k+ pointsStat chartsObservable PlotClean SVG/PNG export for slidesTimelineuPlotFast time-series, Canvas-basedWire formatApache Arrow + progressive loadingJSON breaks at this scaleVideoHTML5 <video> + byte-range MP4Don't re-engineer their pipelineThumbnailsCDN-served images, sprite sheets per clusterThis is what CDNs are forBFF (if needed)FastAPI or Node on Cloud Run / Fly.io / their K8sPersistent processes, not serverlessServer stateTanStack QueryCaching is load-bearing hereClient stateZustandSelection / naming / view state only